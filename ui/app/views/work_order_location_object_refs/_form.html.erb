<%= form_for(@work_order_location_object_ref) do |f| %>
  <% if @work_order_location_object_ref.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_order_location_object_ref.errors.count, "error") %> prohibited this work_order_location_object_ref from being saved:</h2>

      <ul>
      <% @work_order_location_object_ref.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_order_id %><br>
    <%= f.text_field :work_order_id %>
  </div>
  <div class="field">
    <%= f.label :loc_ref_object_id %><br>
    <%= f.text_field :loc_ref_object_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
