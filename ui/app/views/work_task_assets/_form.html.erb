<%= form_for(@work_task_asset) do |f| %>
  <% if @work_task_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_task_asset.errors.count, "error") %> prohibited this work_task_asset from being saved:</h2>

      <ul>
      <% @work_task_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_task_id %><br>
    <%= f.text_field :work_task_id %>
  </div>
  <div class="field">
    <%= f.label :asset_id %><br>
    <%= f.text_field :asset_id %>
  </div>
  <div class="field">
    <%= f.label :created_by %><br>
    <%= f.text_field :created_by %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
