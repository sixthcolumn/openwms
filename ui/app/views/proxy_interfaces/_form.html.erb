<%= form_for(@proxy_interface) do |f| %>
  <% if @proxy_interface.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proxy_interface.errors.count, "error") %> prohibited this proxy_interface from being saved:</h2>

      <ul>
      <% @proxy_interface.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :interface_id %><br>
    <%= f.text_field :interface_id %>
  </div>
  <div class="field">
    <%= f.label :proxy_url %><br>
    <%= f.text_field :proxy_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
