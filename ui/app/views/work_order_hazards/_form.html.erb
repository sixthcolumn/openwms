<%= form_for(@work_order_hazard) do |f| %>
  <% if @work_order_hazard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_order_hazard.errors.count, "error") %> prohibited this work_order_hazard from being saved:</h2>

      <ul>
      <% @work_order_hazard.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_order_id %><br>
    <%= f.text_field :work_order_id %>
  </div>
  <div class="field">
    <%= f.label :hazard_id %><br>
    <%= f.text_field :hazard_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
