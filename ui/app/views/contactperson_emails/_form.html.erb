<%= form_for(@contactperson_email) do |f| %>
  <% if @contactperson_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contactperson_email.errors.count, "error") %> prohibited this contactperson_email from being saved:</h2>

      <ul>
      <% @contactperson_email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contactperson_id %><br>
    <%= f.text_field :contactperson_id %>
  </div>
  <div class="field">
    <%= f.label :email_id %><br>
    <%= f.text_field :email_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
