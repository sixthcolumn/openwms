<h1>Listing interfaces</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Package</th>
      <th>Proxy flag</th>
      <th>Required flag</th>
      <th>Vendor role type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @interfaces.each do |interface| %>
      <tr>
        <td><%= interface.name %></td>
        <td><%= interface.package %></td>
        <td><%= interface.proxy_flag %></td>
        <td><%= interface.required_flag %></td>
        <td><%= interface.vendor_role_type %></td>
        <td><%= link_to 'Show', interface %></td>
        <td><%= link_to 'Edit', edit_interface_path(interface) %></td>
        <td><%= link_to 'Destroy', interface, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Interface', new_interface_path %>
