<%= form_for(@work_order_name) do |f| %>
  <% if @work_order_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_order_name.errors.count, "error") %> prohibited this work_order_name from being saved:</h2>

      <ul>
      <% @work_order_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :name_type_id %><br>
    <%= f.text_field :name_type_id %>
  </div>
  <div class="field">
    <%= f.label :work_order_id %><br>
    <%= f.text_field :work_order_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
