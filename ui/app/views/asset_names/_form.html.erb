<%= form_for(@asset_name) do |f| %>
  <% if @asset_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asset_name.errors.count, "error") %> prohibited this asset_name from being saved:</h2>

      <ul>
      <% @asset_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asset_id %><br>
    <%= f.text_field :asset_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :name_type_id %><br>
    <%= f.text_field :name_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
