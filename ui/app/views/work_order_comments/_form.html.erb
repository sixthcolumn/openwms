<%= form_for(@work_order_comment) do |f| %>
  <% if @work_order_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_order_comment.errors.count, "error") %> prohibited this work_order_comment from being saved:</h2>

      <ul>
      <% @work_order_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_order_id %><br>
    <%= f.text_field :work_order_id %>
  </div>
  <div class="field">
    <%= f.label :comment_id %><br>
    <%= f.text_field :comment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
