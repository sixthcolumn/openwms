<%= form_for(@resultcode) do |f| %>
  <% if @resultcode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resultcode.errors.count, "error") %> prohibited this resultcode from being saved:</h2>

      <ul>
      <% @resultcode.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
