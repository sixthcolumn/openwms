<%= form_for(@asset) do |f| %>
  <% if @asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@asset.errors.count, "error") %> prohibited this asset from being saved:</h2>

      <ul>
      <% @asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mRID %><br>
    <%= f.text_field :mRID %>
  </div>
  <div class="field">
    <%= f.label :critical_flag %><br>
    <%= f.number_field :critical_flag %>
  </div>
  <div class="field">
    <%= f.label :utc_number %><br>
    <%= f.text_field :utc_number %>
  </div>
  <div class="field">
    <%= f.label :address_id %><br>
    <%= f.text_field :address_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
